//rectangle area

{
	let a = 10,
		b = 5;
	if (a >= 0 && b >= 0) {
		let O, P;
		O = 2 * a + 2 * b;
		P = a * b;
		console.log(O, P);
	}
}
{
	let a = 3;
	if (a >= 0) {
		let O, P;
		O = 4 * a;
		P = a * a;
		console.log(O, P);
	}
}
